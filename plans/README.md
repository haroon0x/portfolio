# Implementation Plans

Generated by the improve skill on 2026-06-11 at commit `dbcfd78`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

Full audit detail (all findings, evidence, rejected items, backlog) lives in
[audit-findings.md](audit-findings.md).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Dark Linear-style redesign (tokens, motion, hero, PR page, favicon/OG, tests) | P1 | L | — (but see notes below) | DONE |
| 002 | Light anime "white lily garden" redesign (full identity swap, same functional fixes) | P1 | L | — (conflicts with 001) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **Plans 001 and 002 are mutually exclusive.** They restyle the same files
  toward two different visual identities (001: refined dark Linear-style;
  002: light anime lily-garden). Pick ONE, mark the other REJECTED with
  rationale. Both fold in the same functional fixes (dead theme system, fake
  loader, Hero mousemove re-renders, Lenis options, favicon/OG, contrast,
  added e2e tests), so whichever runs, the other's functional value is
  covered.

- **Strong recommendation**: land the repo-hygiene fixes from
  `audit-findings.md` (DX-01 `.gitattributes` + line-ending normalization,
  DEBT-01 untrack `haroon-resume.html` and stray binaries) **before** plan
  001 — otherwise its diff is buried under whole-repo CRLF churn. No plan
  file exists for them yet; ask the advisor to write one, or treat them as a
  10-minute manual chore.

## Backlog (findings without plans yet)

From `audit-findings.md`: DEBT-01 (untrack resume/junk), DX-01
(.gitattributes), SECURITY-01 (`npm audit fix`), CORRECTNESS-02 (fetch-prs
concurrency), DX-02 (no-change commit noise), SECURITY-02 (webhook stub:
delete or harden), DEBT-03 partial (handled in 001), TESTS-01 (script tests),
DIRECTION-01 (auto-discover PRs), DIRECTION-03 (webhook real-time refresh).

## Findings considered and rejected

- React 19 / Tailwind 4 migrations: cost exceeds payoff for a working static
  portfolio; revisit when forced by a dependency.
- `vite.config.ts` dev-server cache header: dev-only oddity, harmless.
- README project-list drift: trivial; fix opportunistically.
